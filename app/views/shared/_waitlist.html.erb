<% if @waitlist_items.any? %>

<% if logged_in?  && @restaurant.users.include?(current_user) %>
<table class="table">
  <tr class="parties">
  <th>Name</th>
  <th>Size</th>
  <th>Phone</th>
  <th>Waiting</th>
  <th>Restaurant</th>
  <th>Edit</th>
  <th>Seat/Delete</th>
  <th>Notify</th>
  </tr>
    <%= render @waitlist_items %>
</table>
  <%= will_paginate @waitlist_items %>
<% else %>

<table class="table">
  <tr class="parties">
  <th>Size</th>
  <th>Waiting</th>
  </tr>
    <%= render @waitlist_items %>
</table>
<% end %>

<% end %>
